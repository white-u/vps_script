# Linux 端口流量管理脚本 (Port Monitor & Shaper)

**版本**: v5.6.4 | **快捷命令**: `pm` | **安装路径**: `/usr/local/bin/pm`

---

## 1. 概述

pm.sh 是一个基于 **Nftables + TC** 的端口级流量管理工具，适用于多用户 VPS 场景。它支持流量统计、配额管理、自动封禁，以及按需接入的固定突发限速保护。

**v5.6.4 Telegram 模板与菜单整理:**
*   **精简统计卡片:** Worker v3.2 合并流量与百分比，拆分到期信息，删除重复标题和进度条，减少多节点报告的视觉噪声。
*   **按钮闭环:** `/ll` 使用节点按钮，详情页提供返回与刷新；`/help`、`/users` 可通过按钮进入查询、权限说明和 TG-ID 页面。
*   **权限操作引导:** 用户可用 `/id` 获取自己的 TG-ID；新增权限会验证节点中的真实端口并说明覆盖范围，删除权限必须追加 `--yes` 才会执行。
*   **统一通知样式:** 本机事件通知统一用户、分组和流量单位格式，Telegram 配置按“配置、开关、阈值、API、测试”排列。
*   **无额外负载:** 只调整消息生成和交互按钮，不增加 Cron、采样或常驻进程。

**v5.6.3 终端布局优化:**
*   **紧凑端口卡片:** 主菜单使用两行展示每个端口，不再依赖容易受中文和颜色码影响的固定列宽。
*   **简洁服务扫描:** 活跃端口、协议、进程和监控状态改用分隔式列表，长进程名不会再挤乱其他字段。
*   **统一交互提示:** 所有菜单使用一致的分隔线；有默认行为的输入明确标注默认值，危险操作默认取消。
*   **清晰接入列表:** `pm --ipl` 按端口分段展示 TCP 接入 IP，不再使用容易错位的制表符表格。

**v5.6.2 通知菜单整理:**
*   **统一入口:** 主菜单将 Telegram 告警和 Cloudflare 上报合并到“通知与推送”。
*   **原子配置:** Bot Token 与 Chat ID 在同一流程中配置，避免只更新一个字段。
*   **精简通知:** 删除 Telegram 配额重置通知和定时流量报告；配额清零及 Cloudflare 上报逻辑不受影响。

**v5.6.1 接入监控与固定突发限速:**
*   **添加时选择:** 添加端口时询问是否接入，直接回车默认不接入。
*   **固定规则:** 出站平均速率连续 20 分钟超过 300 Mbps 后，临时限制为 50 Mbps，5 分钟后恢复不限速。
*   **连续判定:** 任意采样周期的出站平均速率不超过 300 Mbps，连续累计立即清零；恢复后重新累计。
*   **限速通知:** 已开启 Telegram 时，50 Mbps 规则成功生效后发送触发通知，恢复不限速后发送恢复通知。
*   **取消基础限速:** 不再设置长期基础限速。升级会移除旧 `limit_mbps` 和自定义 QoS 参数，原来已启用 QoS 的端口保留“已接入”状态并改用固定规则。
*   **默认 IP 监控:** 新增端口自动启用仅告警模式；4–5 个 IP 发送普通告警，6 个及以上立即发送高风险升级告警，同级持续超限每 30 分钟最多提醒一次。

**v5.5.2 轻量限速分类:**
*   **无 Packet Mark:** Nftables 通过 `classid` map 直接设置 TC priority，不再改写 packet mark。
*   **无 TC Filter:** HTB 直接按 classid 分类，不再为每个端口创建 IPv4/IPv6 filter。
*   **无隐性带宽上限:** 未限速流量走 HTB direct queue，不再经过固定 1000 Mbps 默认分类。
*   **自动迁移:** 首次运行新版会清理 PM 自有的旧 mark/filter/默认分类并重建限速规则。

**v5.5.1 可靠性改进:**
*   **规则操作可回滚:** 添加、删除端口或修改限速失败时会打印错误并恢复原配置，不再显示虚假的成功结果。
*   **状态安全写入:** 运行状态采用原子替换并按数据解析；意外中断不会留下半个状态文件，也不会把状态内容当作 Shell 代码执行。
*   **无残留删除:** 删除端口会同步清理对应 TC class/分类映射；完整卸载只删除 PM 自己登记的 TC 根队列。
*   **真正互斥:** 菜单操作与每分钟后台任务共用文件锁，避免同时更新时覆盖流量状态。

**v5.4 新特性:**
*   **配置自动迁移 (Auto-Migration):** 脚本现在具备自我维护能力。升级时会自动检测配置文件版本，并平滑迁移旧数据结构，无需人工干预。
*   **分组流量聚合:** 支持多端口共享配额。
*   **并发安全:** 严格的文件锁机制，防止 Cron 与人工操作冲突。

### 1.1 核心功能

*   **流量统计:** 精确统计每个端口的入站/出站流量 (Bytes)。
*   **分组管理:** 多端口流量合并计算，同组端口共享配额。
*   **配额管理:** 设定月流量配额 (GB)，超额自动封禁。
*   **突发限速:** 用户可按端口接入固定防滥用策略，未接入端口始终不限速。
*   **通知系统:** Telegram Bot + Cloudflare Worker 推送。
*   **用户到期提醒:** 可为每个端口设置 `YYYY-MM-DD` 到期日期，到期前 3 天通过本机 Telegram 通知提醒一次，不会自动封禁或删除用户。
*   **统一 TG 样式:** 查询、权限管理、流量预警、突发限速和接入告警使用一致的卡片式布局；超长报告会按行自动分段发送。
*   **在线 IP 上报:** 云端推送会附带每个监控端口当前连接 IP 数和 IP 列表；Telegram 查询时会尝试显示归属地/运营商。
*   **低负载采集:** 正常每轮后台任务只抓取一次 TCP 连接快照，供在线上报和 IP Sentinel 共用；主动阻断后会重新采样以保证上报准确。归属地使用批量查询、超时降级与限流退避。

---

## 2. 安装与使用

### 2.1 一键安装

```bash
bash <(curl -fsSL https://raw.githubusercontent.com/white-u/vps_script/main/pm.sh)
```

### 2.2 常用命令

| 命令 | 说明 |
|------|------|
| `pm` | 进入交互式管理菜单 (主入口) |
| `pm update` | 检查并更新到最新版本 |
| `pm uninstall` | 卸载脚本及清除所有规则 |

### 2.3 交互菜单

运行 `pm` 后：

```text
==============================================================================
  Linux 端口流量管理 v5.6.4
  每分钟刷新 · 编辑期间后台任务暂停
==============================================================================
 [1] 10001 · 双向 · 分组 vip1 · 用户 Alice
     流量 4.2 GB / 100 GB · 1 日重置 · 突发 已接入

 [2] 10002 · 双向 · 分组 vip1 · 用户 Bob
     流量 4.2 GB / 100 GB · 1 日重置 · 突发 50 Mbps（生效中）

 [3] 20000 · 仅出站 · 用户 Carol
     流量 [已阻断] / 5 GB · 突发 未接入
------------------------------------------------------------------------------
 提示：分组端口显示组内合计流量。
```

带默认行为的输入会直接标注，例如 `是否接入突发限速？ [y/N，默认 N]:`；直接回车采用显示的默认值。删除、清零等危险操作统一默认取消。

### 2.4 通知与推送

主菜单选择 `4. 通知与推送` 后，可分别进入 Telegram 通知和 Cloudflare 云端推送。Telegram 负责流量阈值、配额耗尽、突发限速、IP 接入及用户到期提醒；不再发送配额自动重置通知，也不再提供定时流量报告。Cloudflare 只负责节点数据上报和 Bot 查询。

Telegram 通知菜单按常用配置顺序排列：

```text
1. 配置 Bot（Token + Chat ID）
2. 开启/关闭通知
3. 设置 流量提醒阈值
4. 设置 API 地址
5. 发送测试消息
0. 返回通知与推送
```

---

## 3. 分组流量聚合 (Group Traffic)

### 3.1 功能简介

允许将多个端口（如 `10001` 和 `10002`）绑定到同一个组（如 `vip1`）。
*   **流量共享:** 组内所有端口的流量合并计算。
*   **统一封禁:** 一旦总流量超过配额，整组端口同时封禁。
*   **自动同步:** 修改组内任意一个端口的配额或重置日，会自动同步给全组，防止逻辑冲突。

### 3.2 配置方法

1.  在主菜单选择 `2. 配置 端口`。
2.  选择目标端口。
3.  选择 `7. 设置/修改 分组 ID`。
    *   **选择现有组:** 脚本会列出已存在的组名，直接输入序号即可加入。
    *   **新建组:** 输入新的组名（支持字母、数字、下划线）。
    *   **退出组:** 留空回车即可清除分组。

---

## 4. 技术细节

### 4.1 用户到期提醒

1. 运行 `pm`（也可从 `vt` -> `3. 端口流量监控` 进入），选择 `2. 配置 端口`。
2. 选择用户对应的端口，再选择 `9. 设置 用户到期提醒`。
3. 输入完整日期，例如 `2026-09-09`。输入 `0` 可关闭。

提醒复用 PM 现有的每分钟后台任务，不增加 Cron。同一端口和到期日只发送一次；若节点在提前 3 天时离线，只要在到期日前恢复运行，本机 Telegram 会补发。Telegram 未开启或配置不完整时不会记录为已提醒，配置完成后仍可在到期窗口内发送。日期计算统一使用 UTC 日界线。

### 4.2 IP 接入告警

新增端口默认开启 IP Sentinel，允许 3 个独立 IP，处理策略为“仅告警”，不会自动断开连接。已开启 Telegram 时，4–5 个 IP 发送普通超限告警，6 个及以上立即追加一次高风险升级告警；同一等级持续超限每 30 分钟最多提醒一次。连接数回落到 3 个及以下后，本轮告警状态自动清除。

配置菜单可调整允许 IP 数，升级告警线始终是“允许值 + 2”；白名单 IP 不参与计数。该功能复用每分钟的连接快照，不增加 Cron，仅统计当前处于 established 状态的 TCP 对端，UDP 和已断开的历史连接不计入。

可选的“自动断开”会按当前 established TCP 连接数从多到少排列非白名单 IP，保留前 N 个，并通过 `ss -K` 断开其余 IP 在该端口上的连接。断开判断每分钟执行，不受 Telegram 的 30 分钟通知冷却影响。此规则是真实执行，但无法识别哪个 IP 属于合法用户；连接数更多的异常 IP 也可能被保留。因此默认只告警，启用自动断开前应先配置可信白名单。执行失败会记录到 `/var/log/port_monitor.log`，不会静默显示成功；Telegram 未配置或发送失败时不会进入通知冷却，后续轮次仍会尝试补发。

### 4.3 数据迁移系统 (Migration)

从 v5.3.0 开始，`config.json` 引入了版本控制机制。
每次运行脚本时，`migrate_config` 函数会自动检查配置文件的版本：
*   **字段补全:** 自动为旧配置补充缺失的字段（如 `group_id`, `config_version`）。
*   **结构升级:** 如果未来数据结构发生重大变更，脚本会自动将旧数据转换为新格式，确保**无痛升级**。

### 4.4 流量统计原理 (Nftables)

使用 `nft` 的 named counter 功能，在内核态统计流量。每次读取后计算增量 (Delta) 并累加到独立状态文件中，重启不丢失。

### 4.5 固定突发限速原理 (TC)

*   **HTB 队列:** 使用 Linux TC 的 HTB 队列进行流量控制。
*   **Classid Map:** 仅将实际限速的端口加入 Nftables `inet_service : classid` map。
*   **TC Priority:** Nftables 直接设置包的 TC priority，HTB 按 classid 进入对应限速 class，无需 packet mark 或 TC filter。

未处于 5 分钟限速期时不会创建 TC 根队列；“已接入”只启用每分钟的出站速率判断，不会长期改变带宽。触发后才创建并登记 PM 自有的 `HTB 1:` 根队列与 50 Mbps class，恢复时会删除对应 class；最后一个限速端口恢复后同步删除 PM 根队列。根队列不设默认限速 class，未命中的其他流量走 direct queue。如果接口上已存在其他程序管理的同名根队列，PM 会拒绝接管并明确报错。

---

## 5. 故障排查

后台任务、Telegram 通知和 Worker 推送错误会写入 `/var/log/port_monitor.log`：

```bash
tail -n 100 /var/log/port_monitor.log
```

**Q: 升级后配置文件会不会丢？**
A: 不会。新的迁移系统会保护你的数据。即使脚本结构大改，它也会尝试兼容旧数据。

**Q: 为什么我修改配额时没有提示我是否同步？**
A: 为了保证数据一致性，v5.2+ 版本采取了**强制同步**策略。只要端口在组里，修改配额会自动覆盖全组，不再反复询问。

---

## 6. 卸载

在菜单中选择 `6. 卸载脚本`，或运行 `pm uninstall`。
卸载会自动清理本脚本的 Cron 任务、Nftables 规则，以及由本脚本创建并登记的 TC 根队列和所有配置文件；其他程序创建的 TC 根队列会保留。任一核心清理步骤失败时会停止卸载、打印错误并保留配置，便于排查后重试。
